let renderDesign = (fileName) => {
    var d = document.createElement('canvas');
    d.setAttribute('width', '500%');
    d.setAttribute('height', '500%');
    d.setAttribute('class', 'img-fluid');
    
};
